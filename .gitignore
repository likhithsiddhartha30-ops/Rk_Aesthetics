# OS / editor noise
Thumbs.db
desktop.ini
.DS_Store

# Secrets — never commit. Set these with `supabase secrets set` instead.
.env
.env.*
supabase/.env
supabase/.temp/

# Paid products. Served from the private Supabase bucket, never from the site.
Products/
